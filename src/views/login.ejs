
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login Page</title>
    <!-- Font Awesome untuk ikon -->
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"
    />
    <!-- Google Font -->
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <style>
      * {
        font-family: "Poppins", sans-serif;
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
  background: url("/school-bro.png");
  width: 100%;
  height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  background-size: cover;
  background-position: center;
  position: relative; /* Tambahkan ini */
}

body::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.1); /* Overlay gelap */
  z-index: 0; /* Pastikan overlay berada di bawah konten */
}

.container {
  position: relative; /* Pastikan kontainer tetap di atas overlay */
  z-index: 1;
  background: rgba(0, 0, 0, 0.5);
  width: 400px;
  padding: 32px;
  border-radius: 16px;
  border: solid 5px rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(25px);
  box-shadow: 0px 4px 30px rgba(0, 0, 0, 0.2);
  color: white;
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
}

      .login-title {
        margin-bottom: 24px;
        font-size: 24px;
        font-weight: bold;
      }

      .input-box {
        display: flex;
        width: 100%;
        position: relative;
        margin-top: 20px;
      }

      .input-box input {
        width: 100%;
        padding: 12px 16px 12px 45px;
        border-radius: 99px;
        border: solid 3px transparent;
        background: rgba(255, 255, 255, 0.1);
        outline: none;
        caret-color: white;
        color: white;
        font-weight: 500;
        transition: 0.25s;
      }

      .input-box input:focus {
        border: solid 3px rgba(255, 255, 255, 0.4);
        background: rgba(255, 255, 255, 0.15);
      }

      .input-box input::placeholder {
        color: rgba(255, 255, 255, 0.75);
      }

      .input-box i {
        position: absolute;
        top: 50%;
        transform: translateY(-50%);
        left: 16px;
        color: rgba(255, 255, 255, 0.75);
        font-size: 18px;
        transition: 0.25s;
      }

      .input-box input:focus + i {
        color: white;
      }

      .remember-forgot-box {
        display: flex;
        width: 100%;
        margin-top: 16px;
        justify-content: space-between;
        align-items: center;
      }

      .remember-me {
        display: flex;
        align-items: center;
        gap: 8px;
      }

      .forgot-password {
        text-decoration: none;
        color: rgba(255, 255, 255, 0.75);
        font-size: 14px;
      }

      .forgot-password:hover {
        text-decoration: underline;
        color: white;
      }

      .login-button {
        width: 100%;
        margin-top: 24px;
        padding: 12px 0;
        background: #32cdd5;
        border: none;
        border-radius: 99px;
        color: white;
        font-weight: bold;
        font-size: 16px;
        cursor: pointer;
        transition: 0.2s;
      }

      .login-button:hover {
        background: #28b4bd;
      }

      .login-button:focus {
        outline: #32cdd58a 3px solid;
      }

      .dont-have-an-account {
        font-weight: normal;
        margin-top: 16px;
        font-size: 14px;
      }

      .dont-have-an-account a {
        color: #32cdd5;
        text-decoration: none;
        font-weight: bold;
      }

      .dont-have-an-account a:hover {
        text-decoration: underline;
      }
    </style>
  </head>
  <body>
    <form action="/login/auth" method="post" class="container">
      <h1 class="login-title">Absensi Siswa <br> <%= sekolah.nama %></h1>
      <section class="input-box">
        <input type="text" placeholder="Username" name="username" />
        <i class="fas fa-user"></i>
      </section>
      <section class="input-box">
        <input type="password" placeholder="Password" name="password"/>
        <i class="fas fa-lock"></i>
      </section>
      <section class="remember-forgot-box">
        <div class="remember-me">
          
        </div>
        
      </section>
      <button class="login-button" type="submit">Login</button>
    </form>
  </body>
</html>
